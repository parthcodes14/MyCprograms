#include <stdio.h>

int main()
{
    // Writing a program to calculate area of a rectangle using hard coded inputs

    int a = 15;
    int b = 25;

    int c = a * b;
    printf("The area of a rectangle with length %d and breadth %d is %d", a, b, c);
    return 0;
}
